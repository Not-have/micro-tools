{
  "name": "tiny-tools",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "scripts": {
    "postinstall": "npx only-allow pnpm",
    "clean": "npx rimraf dist && npx rimraf node_modules && npx rimraf pnpm-lock.yaml && npx rimraf yarn.lock"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages-vue"
  ],
  "devDependencies": {
    "only-allow": "^1.1.1",
    "rimraf": "^5.0.1"
  }
}
